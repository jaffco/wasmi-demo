# Force host adapter transport and safe adapter speed
# This file is loaded before the interface/target configs so the transport
# is selected before OpenOCD auto-selects a session transport.
transport select hla_swd
adapter_khz 1000